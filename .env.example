# Environment Variables Example for Charles AI Workspace
# Copy to .env for local overrides if needed.

# Node Environment
NODE_ENV=production

# Object Storage / CDN Base URL (Leave blank for local static assets)
# Example: https://assets.charlesai.com
PUBLIC_CDN_BASE_URL=
